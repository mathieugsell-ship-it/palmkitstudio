---
// /versions — renders the shared versions index (kept so the "← All versions"
// links on every iteration page keep working). The list lives in the component.
import VersionsIndex from '../components/VersionsIndex.astro';
---

<VersionsIndex />
